/*
#include "movielist.h"

MovieNode::MovieNode(){
	data = NULL;
	next = NULL;
}

MovieList::MovieList(){
	root = NULL;
}

void MovieList::insert(Movie *inserted){
	if(inserted == NULL) return;
	MovieNode* current = root;
}
*/